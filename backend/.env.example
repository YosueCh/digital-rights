# ===========================================
# CONFIGURACIÓN DE DIGITAL RIGHTS
# ===========================================
# IMPORTANTE: Copia este archivo como .env y completa los valores

# --- Servidor ---
PORT=3000
NODE_ENV=development

# --- Base de Datos ---
DB_PATH=./database/digital_rights.db

# --- JWT (Autenticación) ---
# Genera un secreto seguro: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=CAMBIAR_ESTE_SECRETO_EN_PRODUCCION
JWT_EXPIRES_IN=7d

# --- AES-256 (CAPA 2: Cifrado Simétrico) ---
# Genera la llave maestra (32 bytes = 64 caracteres hex)
# Comando: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
AES_MASTER_KEY=

# --- RSA (CAPA 3: Firma Digital) ---
KEYS_DIR=./keys

# --- CORS ---
CLIENT_URL=http://localhost:5173

# --- Uploads ---
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=52428800